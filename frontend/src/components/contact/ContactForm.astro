---
import SectionLabel from "../ui/SectionLabel.astro";
---

<section class="w-full bg-white text-black">
  <div class="site-container py-17.5">
    <div class="grid gap-16 lg:grid-cols-[0.9fr_1.1fr] lg:gap-22.5">
      <div>
        <SectionLabel class="mb-5"> ESCRÍBENOS </SectionLabel>

        <h2
          class="text-[clamp(28px,3vw,40px)] font-bold leading-[1.04] tracking-[-0.04em] text-black"
        >
          Completa el formulario y te contactaremos.
        </h2>

        <p class="mt-5 max-w-110 text-sm leading-[1.65] text-gray-600">
          No necesitas tener todo resuelto: cuéntanos qué está pasando en tu
          operación y analizamos juntos por dónde empezar.
        </p>
      </div>

      <div class="min-h-100 overflow-hidden">
        <div class="-mb-11">
          <script is:inline>
          !(function (a, m, o, c, r, m) {
            ((a[o + c] = a[o + c] || {
              setMeta: function (p) {
                this.params = (this.params || []).concat([p]);
              },
            }),
              (a[o + r] =
                a[o + r] ||
                function (f) {
                  a[o + r].f = (a[o + r].f || []).concat([f]);
                }),
              a[o + r]({
                id: "1727923",
                hash: "1a15546a9d55b780dd12f385c27b2143",
                locale: "es",
              }),
              (a[o + m] =
                a[o + m] ||
                function (f, k) {
                  a[o + m].f = (a[o + m].f || []).concat([[f, k]]);
                }));
          })(window, 0, "amo_forms_", "params", "load", "loaded");
        </script>
        <script
          is:inline
          id="amoforms_script_1727923"
          async="async"
          charset="utf-8"
          src="https://forms.kommo.com/forms/assets/js/amoforms.js?1788558988"
        ></script>
        </div>
      </div>
    </div>
  </div>
</section>