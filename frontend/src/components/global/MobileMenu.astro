<nav class="fixed inset-x-0 top-21 z-45 bg-black border-b border-white/8" data-mobile-menu hidden>
  <div class="flex flex-col gap-5 px-(--container-padding) pt-7 pb-9">
    <a href="/soluciones" class="text-[1.1rem] font-semibold text-white">Soluciones</a>
    <a href="/proyectos" class="text-[1.1rem] font-semibold text-white">Proyectos</a>
    <a href="/nosotros" class="text-[1.1rem] font-semibold text-white">Nosotros</a>
    <a href="/insights" class="text-[1.1rem] font-semibold text-white">Insights</a>
    <a class="mt-3 text-[1.1rem] font-semibold text-blue-light!" href="/contacto">
      Cuéntanos tu proyecto →
    </a>
  </div>
</nav>

<script>
  const toggle = document.querySelector("[data-mobile-menu-toggle]");
  const menu = document.querySelector("[data-mobile-menu]");

  if (toggle && menu) {
    toggle.addEventListener("click", () => {
      const isOpen = toggle.getAttribute("aria-expanded") === "true";

      toggle.setAttribute("aria-expanded", String(!isOpen));
      menu.toggleAttribute("hidden");
    });
  }
</script>
