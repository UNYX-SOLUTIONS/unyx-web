---
const {
  as: Tag = "span",
  class: className = "",
  dark = false,
} = Astro.props;
---

<Tag class:list={["inline-flex items-center gap-2.5 text-xs font-bold uppercase tracking-[0.09em] before:block before:h-px before:w-5 before:bg-current before:content-['']", dark ? "text-blue-light" : "text-blue", className]}>
  <slot />
</Tag>
