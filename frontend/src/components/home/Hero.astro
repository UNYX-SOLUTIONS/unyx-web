<section class="hero" id="inicio">
  <div class="hero__container">

    <div class="hero__content">
      <div class="hero__label">
        <span class="hero__label-dot"></span>
        <span>TECNOLOGÍA · AUTOMATIZACIÓN · INGENIERÍA</span>
      </div>

      <h1 class="hero__title">
        Tecnología aplicada a
        <span>necesidades reales.</span>
      </h1>

      <p class="hero__description">
        Diseñamos e integramos soluciones de software, automatización,
        inteligencia artificial e infraestructura que ayudan a las empresas
        a trabajar mejor.
      </p>

      <div class="hero__actions">
        <a
          class="hero__button hero__button--primary"
          href="/contacto"
        >
          <span>Cuéntanos tu proyecto</span>
          <span class="hero__arrow" aria-hidden="true">→</span>
        </a>

        <a
          class="hero__button hero__button--secondary"
          href="#soluciones"
        >
          Explorar soluciones
        </a>
      </div>
    </div>

    <div class="hero__visual" aria-hidden="true">
      <div class="network">

        <span class="network__line network__line--software"></span>
        <span class="network__line network__line--ai"></span>
        <span class="network__line network__line--infra"></span>
        <span class="network__line network__line--automation"></span>
        <span class="network__line network__line--data"></span>

        <div class="network__node network__node--software">
          SOFTWARE
        </div>

        <div class="network__node network__node--ai">
          IA
        </div>

        <div class="network__node network__node--infra">
          INFRAESTRUCTURA
        </div>

        <div class="network__node network__node--automation">
          AUTOMATIZACIÓN
        </div>

        <div class="network__node network__node--data">
          DATOS
        </div>

        <div class="core">
          <span class="core__cube"></span>
          <span class="core__cube"></span>
          <span class="core__cube"></span>
          <span class="core__cube core__cube--active"></span>
        </div>
      </div>
    </div>

  </div>

  <div class="hero__scroll" aria-hidden="true">
    <span>SCROLL</span>
    <span class="hero__scroll-line"></span>
  </div>
</section>

<style>
  .hero {
    position: relative;

    min-height: 100svh;

    display: flex;
    align-items: center;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 76% 48%,
        rgba(0, 80, 248, 0.045),
        transparent 28%
      ),
      #0b0b0d;

    color: #ffffff;
  }

  .hero::before {
    content: "";

    position: absolute;
    inset: 0;

    pointer-events: none;

    background-image:
      linear-gradient(
        rgba(255, 255, 255, 0.018) 1px,
        transparent 1px
      ),
      linear-gradient(
        90deg,
        rgba(255, 255, 255, 0.018) 1px,
        transparent 1px
      );

    background-size: 44px 44px;

    mask-image: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(0, 0, 0, 0.45) 18%,
      rgba(0, 0, 0, 1) 50%,
      rgba(0, 0, 0, 0.5) 100%
    );

    opacity: 0.55;
  }

  .hero__container {
    position: relative;
    z-index: 2;

    width: min(calc(100% - 48px), 1280px);

    margin-inline: auto;

    display: grid;
    grid-template-columns: 1.05fr 0.95fr;

    gap: 60px;

    align-items: center;

    padding-top: 80px;
    padding-bottom: 100px;

    transform: translateY(-24px);
  }

  .hero__content {
    max-width: 700px;
  }

  .hero__label {
    display: flex;
    align-items: center;

    gap: 10px;

    margin-bottom: 24px;

    color: #8da7d9;

    font-size: 12px;
    font-weight: 700;

    line-height: 1;

    letter-spacing: 0.06em;
  }

  .hero__label-dot {
    width: 7px;
    height: 7px;

    flex: 0 0 auto;

    border-radius: 50%;

    background: #0050f8;

    box-shadow: 0 0 14px rgba(0, 80, 248, 0.65);
  }

  .hero__title {
    max-width: 680px;

    margin: 0;

    color: #ffffff;

    font-size: clamp(48px, 4.7vw, 68px);
    font-weight: 700;

    line-height: 1.02;

    letter-spacing: -0.045em;

    text-wrap: balance;
  }

  .hero__title span {
    display: block;
  }

  .hero__description {
    max-width: 590px;

    margin: 28px 0 0;

    color: #a8acb4;

    font-size: 17px;

    line-height: 1.6;
  }

  .hero__actions {
    display: flex;
    align-items: center;
    flex-wrap: wrap;

    gap: 14px;

    margin-top: 34px;
  }

  .hero__button {
    min-height: 54px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    gap: 16px;

    padding: 0 24px;

    border-radius: 8px;

    font-size: 14px;
    font-weight: 600;

    line-height: 1;

    text-decoration: none;

    transition:
      background-color 180ms ease,
      border-color 180ms ease,
      color 180ms ease,
      transform 180ms ease;
  }

  .hero__button--primary {
    background: #0050f8;

    border: 1px solid #0050f8;

    color: #ffffff;
  }

  .hero__button--primary:hover {
    background: #0b5cff;

    border-color: #0b5cff;

    transform: translateY(-2px);
  }

  .hero__button--secondary {
    background: transparent;

    border: 1px solid #3c3f45;

    color: #ffffff;
  }

  .hero__button--secondary:hover {
    border-color: #777b84;

    background: rgba(255, 255, 255, 0.035);

    transform: translateY(-2px);
  }

  .hero__arrow {
    font-size: 20px;

    transition: transform 180ms ease;
  }

  .hero__button--primary:hover .hero__arrow {
    transform: translateX(4px);
  }

  .hero__visual {
    position: relative;

    min-height: 520px;

    display: flex;
    align-items: center;
    justify-content: center;

    transform: translateX(-20px);
  }

  .network {
    position: relative;

    width: min(118%, 610px);

    aspect-ratio: 1 / 1;
  }

  .network::before {
    content: "";

    position: absolute;

    inset: 7%;

    background-image: radial-gradient(
      rgba(255, 255, 255, 0.11) 0.8px,
      transparent 0.8px
    );

    background-size: 22px 22px;

    opacity: 0.22;
  }

  .core {
    position: absolute;

    left: 50%;
    top: 50%;

    width: 108px;
    height: 108px;

    display: grid;

    grid-template-columns: repeat(2, 1fr);
    grid-template-rows: repeat(2, 1fr);

    gap: 8px;

    transform: translate(-50%, -50%);

    z-index: 4;
  }

  .core__cube {
    display: block;

    border: 1px solid #41444a;

    border-radius: 5px;

    background: #1a1c20;

    box-shadow:
      inset 0 0 18px rgba(255, 255, 255, 0.015),
      0 4px 18px rgba(0, 0, 0, 0.2);
  }

  .core__cube--active {
    border-color: #0050f8;

    background: #0050f8;

    box-shadow:
      0 0 24px rgba(0, 80, 248, 0.22);
  }

  .network__node {
    position: absolute;

    z-index: 5;

    padding: 6px 9px;

    border: 1px solid rgba(255, 255, 255, 0.17);

    border-radius: 5px;

    background: rgba(11, 11, 13, 0.86);

    color: #8f939b;

    font-size: 10px;
    font-weight: 700;

    line-height: 1;

    letter-spacing: 0.05em;

    white-space: nowrap;
  }

  .network__node--software {
    top: 5%;
    left: 26%;
  }

  .network__node--ai {
    top: 29%;
    right: 1%;
  }

  .network__node--infra {
    left: 0;
    bottom: 25%;
  }

  .network__node--automation {
    right: 0;
    bottom: 13%;
  }

  .network__node--data {
    left: 46%;
    bottom: 1%;
  }

  .network__line {
    position: absolute;

    left: 50%;
    top: 50%;

    z-index: 1;

    height: 1px;

    background: rgba(255, 255, 255, 0.14);

    transform-origin: left center;
  }

  .network__line::after {
    content: "";

    position: absolute;

    right: -3px;
    top: 50%;

    width: 6px;
    height: 6px;

    border-radius: 50%;

    background: #555a63;

    transform: translateY(-50%);
  }

  .network__line--software {
    width: 245px;

    transform: rotate(-115deg);
  }

  .network__line--ai {
    width: 240px;

    background: #0050f8;

    transform: rotate(-34deg);
  }

  .network__line--ai::after {
    background: #0050f8;

    box-shadow: 0 0 10px rgba(0, 80, 248, 0.5);
  }

  .network__line--infra {
    width: 235px;

    transform: rotate(151deg);
  }

  .network__line--automation {
    width: 250px;

    transform: rotate(43deg);
  }

  .network__line--data {
    width: 235px;

    background: rgba(0, 80, 248, 0.65);

    transform: rotate(91deg);
  }

  .network__line--data::after {
    background: #0050f8;
  }

  .hero__scroll {
    position: absolute;

    z-index: 3;

    left: 50%;
    bottom: 24px;

    display: flex;
    flex-direction: column;
    align-items: center;

    gap: 10px;

    transform: translateX(-50%);

    color: #777b84;

    font-size: 9px;
    font-weight: 600;

    letter-spacing: 0.12em;
  }

  .hero__scroll-line {
    display: block;

    width: 1px;
    height: 36px;

    background: linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0.45),
      rgba(255, 255, 255, 0)
    );
  }

  @media (max-width: 1050px) {
    .hero__container {
      grid-template-columns: 1fr 0.78fr;

      gap: 40px;
    }

    .hero__title {
      font-size: clamp(46px, 5.5vw, 62px);
    }

    .hero__visual {
      min-height: 440px;

      transform: translateX(-6px);
    }

    .network {
      width: min(100%, 480px);
    }

    .core {
      width: 96px;
      height: 96px;
      gap: 7px;
    }
  }

  @media (max-width: 800px) {
    .hero {
      min-height: auto;
    }

    .hero__container {
      width: min(calc(100% - 36px), 1280px);

      grid-template-columns: 1fr;

      gap: 54px;

      padding-top: 140px;
      padding-bottom: 90px;

      transform: none;
    }

    .hero__content {
      max-width: 100%;
    }

    .hero__title {
      max-width: 650px;

      font-size: clamp(42px, 12vw, 58px);
    }

    .hero__description {
      max-width: 560px;

      font-size: 16px;
    }

    .hero__visual {
      min-height: auto;

      transform: none;
    }

    .network {
      width: min(100%, 440px);

      margin-inline: auto;
    }

    .core {
      width: 90px;
      height: 90px;
    }

    .hero__scroll {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .hero__container {
      width: calc(100% - 32px);

      padding-top: 125px;
      padding-bottom: 70px;
    }

    .hero__label {
      font-size: 10px;
    }

    .hero__title {
      font-size: clamp(40px, 13vw, 50px);
    }

    .hero__actions {
      align-items: stretch;
      flex-direction: column;
    }

    .hero__button {
      width: 100%;
    }

    .hero__visual {
      margin-top: 10px;
    }

    .network__node {
      font-size: 8px;
    }

    .core {
      width: 82px;
      height: 82px;
      gap: 6px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__button,
    .hero__arrow {
      transition: none;
    }
  }
</style>